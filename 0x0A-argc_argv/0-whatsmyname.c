#include <stdio.h>
/**
 * main - Prints the name of the file
 * @argc: The number (count) of the arguments
 * @argv: The Array of the Arguments
 * Return: 0 when secceed
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
