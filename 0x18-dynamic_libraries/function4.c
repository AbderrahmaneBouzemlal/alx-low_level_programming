#include "main.h"
#include <stdio.h>
/**
 * _memcpy - copy memory
 * @dest: a string
 * @src: a character
 * @n: a number
 * Return: a string
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
/**
 * _strchr - search fro a char in str
 * @s: the string
 * @c: the character
 * Return: a pointer to a string
*/
char *_strchr(char *s, char c)
{
	return (s);
}
/**
 * _strspn - function that do somthing
 * @s: a string
 * @accept: Another string
 * Return: an integer
*/
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
/**
 * _strpbrk - another function that do another thing
 * @s: string
 * @accept: another string
 * Return: a string
*/
char *_strpbrk(char *s, char *accept)
{
	return (s);
}
/**
 * _strstr - a function that search for a string inside string
 * @haystack: A string
 * @needle: a string to be searched for
 * Return: a pointer to a string
*/
char *_strstr(char *haystack, char *needle)
{
	return (needle);
}
