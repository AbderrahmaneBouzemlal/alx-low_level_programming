#include <stdio.h>
/**
 * _strcat - a function to concatinate two strings
 * @dest: a string
 * @src: a string
 * Return: the result of the function
 */
char *_strcat(char *dest, char *src)
{
	return (dest);
}
/**
 * _strncat - a function to concatinate two strings
 * @dest: a string
 * @src: a string
 * @n: the number of bytes
 * Return: the result of the function
 */
char *_strncat(char *dest, char *src, int n)
{
	return (dest);
}
/**
 * _strncpy - a function to cpy two strings
 * @dest: a string
 * @src: a string
 * @n: the number of bytes
 * Return: the result of the function
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (dest);
}
/**
 * _strcmp - a function to compaire between two strings
 * @s1: a string
 * @s2: a string
 * Return: the result of the function
 */
int _strcmp(char *s1, char *s2)
{
	return (0);
}
/**
 * _memset - the function from the c library
 * @s: a string
 * @b: a character
 * @n: an integer
 * Return: a string or NULL in failure
*/
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}

